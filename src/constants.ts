export const ICON_SIZE = '1.3rem';

// Priority queue treatment of facts
export const FACT_PRIO = 30;

// Priority queue treatment of prefixes that might derive new choices
export const CHOICE_PRIO = 10;

// Demands are checked late
export const DEMAND_PRIO = 20;

// Priority queue treatment of prefixes that derive new facts
export const PREFIX_PRIO = 40;

// Priority queue treatment of prefixes that drive towards contradition
export const CONSTRAINT_PRIO = 50;

export const INITIAL_PREFIX_PRIO = 90;

export const LS_SESSION_DIVIDER_PROPORTION = 'dusa-divider-position';
